use v6;

class IO is also {
    method readline($prompt = '> ') {
        $!PIO.readline_interactive($prompt) // Mu;
    }
}

loop {
    my $x = $*IN.readline;
    if !defined $x {
        say '';
        last;
    }
    say $x;
}
